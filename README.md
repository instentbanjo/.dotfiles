# Nvim
Is a submodule linking to my [nvim config](https://github.com/instentbanjo/nvim)
```bash
stow -d ~/.dotfiles/core -t ~ nvim
```

# Ghostty
The best terminal emulator in town
```bash
stow -d ~/.dotfiles/core -t ~ ghostty
```

# .zshrc & .p10k.zsh

```bash
stow -d ~/.dotfiles/core -t ~ zsh
```

